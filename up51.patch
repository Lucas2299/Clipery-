diff --git a/clipery/public/index.html b/clipery/public/index.html
index e73f1bd..6c0335a 100644
--- a/clipery/public/index.html
+++ b/clipery/public/index.html
@@ -85,8 +85,10 @@
       .nav-links a{color:var(--muted);font-size:.9rem;font-weight:500;transition:color .2s}
       .nav-links a:hover{color:var(--text)}
       .nav-cta{display:flex;gap:.55rem;align-items:center}
+      .nav-right{display:flex;gap:.55rem;align-items:center}
       .nav-logout{font-family:inherit;cursor:pointer}
       .nav-acct-txt{font-size:.85rem;font-weight:700}
+      .nav-acct[hidden]{display:none}
       .nav-acct{height:36px;padding:0 .8rem 0 .6rem;gap:.4rem;border-radius:999px;border:1px solid rgba(255,255,255,.22);background:rgba(255,255,255,.06);color:#f4f1ea;display:inline-flex;align-items:center;justify-content:center;text-decoration:none;flex:none}
       .nav-acct:hover{border-color:#ff8a4c;background:rgba(255,138,76,.12)}
       .nav-acct svg{width:18px;height:18px;display:block}
@@ -280,7 +282,7 @@
       }
     </style>
   </head>
-  <body data-auth-modal>
+  <body data-auth-modal data-ui="up51">
     <div class="bg-live" aria-hidden="true">
       <div class="bg-pic"></div>
       <canvas id="bgfx"></canvas>
@@ -299,9 +301,15 @@
           <a href="#features">Features</a>
           <a href="#pricing">Pricing</a>
         </nav>
-        <div class="nav-cta" id="nav-cta">
-          <a class="btn btn-ghost" href="/login">Log in</a>
-          <a class="btn btn-primary" href="/register">Start free</a>
+        <div class="nav-right">
+          <div class="nav-cta" id="nav-cta">
+            <a class="btn btn-ghost" href="/login">Log in</a>
+            <a class="btn btn-primary" href="/register">Start free</a>
+          </div>
+          <a class="nav-acct" id="nav-acct" href="/account" hidden aria-label="Account" title="Account">
+            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 3.6-7 8-7s8 3 8 7"/></svg>
+            <span class="nav-acct-txt">Account</span>
+          </a>
         </div>
       </div>
     </header>
@@ -756,10 +764,13 @@
             cta.innerHTML =
               '<a class="btn btn-ghost" href="/library">Library</a>' +
               '<a class="btn btn-primary" href="/studio">Open Studio</a>' +
-              '<button type="button" class="btn btn-ghost nav-logout" id="nav-logout">Log out</button>' +
-              '<a class="nav-acct" href="/account" title="Account - ' + (u.planLabel || "Free") + " plan, " + left + '" aria-label="Account">' +
-              '<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="4"/><path d="M4 21c0-4 3.6-7 8-7s8 3 8 7"/></svg>' +
-              '<span class="nav-acct-txt">Account</span></a>';
+              '<button type="button" class="btn btn-ghost nav-logout" id="nav-logout">Log out</button>';
+            var acct = document.getElementById("nav-acct");
+            if (acct) {
+              acct.hidden = false;
+              acct.style.display = "inline-flex";
+              acct.title = "Account - " + (u.planLabel || "Free") + " plan, " + left;
+            }
             document.getElementById("nav-logout").addEventListener("click", function () {
               var b = this;
               b.disabled = true;
