diff --git a/clipery/lib/clipEngine.js b/clipery/lib/clipEngine.js
index a5f7421..8eb16a0 100644
--- a/clipery/lib/clipEngine.js
+++ b/clipery/lib/clipEngine.js
@@ -1286,9 +1286,8 @@ async function processVideo(sourcePath, options = {}) {
           hookStyle: options.hookStyle || "boxdark",
           hookColor: options.hookColor || "white",
           hookPos: options.hookPos || "top",
-        hookFrame: options.hookFrame || "",
-        hookBg: options.hookBg || "",
           hookFrame: options.hookFrame || "",
+          hookBg: options.hookBg || "",
           trends: options.trends || [],
           maxClips: clipBudget,
           watermark: options.watermark !== false,
diff --git a/clipery/lib/subtitles.js b/clipery/lib/subtitles.js
index 4bd7315..10fa881 100644
--- a/clipery/lib/subtitles.js
+++ b/clipery/lib/subtitles.js
@@ -324,7 +324,7 @@ function resolveHookFrame(lk, frame, bgColor) {
   // user bg color → ASS box behind text
   const bg = SUB_COLORS.hasOwnProperty(bgColor) ? SUB_COLORS[bgColor] : null;
   if (bg) {
-    lk.bg = "&H00" + bg.slice(4,6) + bg.slice(2,4) + bg.slice(0,2); // BGR swap
+    lk.bg = bg; // SUB_COLORS already stores ASS BGR
   }
   const fx = ["\\fad(200,150)"];
   if (F.frame === "bar") {
